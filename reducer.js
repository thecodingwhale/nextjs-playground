import { actionTypes } from './actions'

export const appInitialState = {}

function reducer (state = appInitialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
